import styled from '@emotion/styled';

export const StyledLoginRoot = styled.form({
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    flexDirection: 'column',
});

export const StyledLogo = styled.img({
    width: '35%',
});
